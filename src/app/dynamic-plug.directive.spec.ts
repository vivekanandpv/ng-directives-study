import { DynamicPlugDirective } from './dynamic-plug.directive';

describe('DynamicPlugDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicPlugDirective();
    expect(directive).toBeTruthy();
  });
});
